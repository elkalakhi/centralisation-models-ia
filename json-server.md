create folder exemple 'date'
and create file.json and make object json in file
after that install json server : 
✅ 1. Installer JSON Server globalement
npm install -g json-server
✅ 2. (Optionnel) Installer localement dans un projet
npm install json-server --save-dev
✅ 3. (Optionnel) start json server
json-server --watch data/db.json --port port